---

version: "3.9"


services:
    foodini_api:
        image: foodini:dev
        environment:
            - RELOAD=True
            - DEBUG=True
        volumes:
            - type: bind
              source: ./foodini/
              target: /app/
              read_only: false
